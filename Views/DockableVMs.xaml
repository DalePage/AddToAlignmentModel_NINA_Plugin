<ResourceDictionary     
    x:Class="ADPUK.NINA.AddToAlignmentModel.AddToAlignmentModelImageTab.AlignmentModelVM"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ADPUK.NINA.AddToAlignmentModel.AddToAlignmentModelImageTab"
    xmlns:ninactrl="clr-namespace:NINA.CustomControlLibrary;assembly=NINA.CustomControlLibrary">
    <DataTemplate x:Key="ADPUK.NINA.AddToAlignmentModel.AddToAlignmentModelImageTab.CreateAlignmentModelVM_Dockable">
        <StackPanel Orientation="Vertical" Margin="10,10,10,10" Width="Auto" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Alignment Model" TextAlignment="Center" Margin="10,10,10,10" FontWeight="DemiBold" FontSize="20"/>
            <TextBlock VerticalAlignment="Center" Text="Number of Azimuth Points: " Margin="10,0,0,0" />
            <TextBox
                        MinWidth="20"
                        Margin="5,0,0,0"
                        TextAlignment="Right"
                        IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="NumberOfAzimuthPoints" UpdateSourceTrigger="LostFocus" />
                </TextBox.Text>
            </TextBox>
            <TextBlock VerticalAlignment="Center" Text="Number of Altitude Points: " Margin="10,0,0,0"/>
            <TextBox
                        MinWidth="20"
                        Margin="5,0,0,0"
                        TextAlignment="Right"
                        IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="NumberOfAltitudePoints"  UpdateSourceTrigger="LostFocus" />
                </TextBox.Text>
            </TextBox>
            <TextBlock VerticalAlignment="Center" Text="Minimum Altidude: " Margin="10,0,0,0" />
            <TextBox
                        MinWidth="20"
                        Margin="5,0,0,0"
                        TextAlignment="Right"
                        IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="MinElevation" UpdateSourceTrigger="LostFocus" />
                </TextBox.Text>
            </TextBox>
            <TextBlock VerticalAlignment="Center" Text="Maximum Altitude: " Margin="10,0,0,0" />
            <TextBox
                        MinWidth="20"
                        Margin="5,0,0,0"
                        TextAlignment="Right"
                        IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="MaxElevation"  UpdateSourceTrigger="LostFocus" />
                </TextBox.Text>
            </TextBox>
            <TextBlock VerticalAlignment="Center" Text="Maximum Solve Attempts: " Margin="10,0,0,0" />
            <TextBox
                        MinWidth="20"
                        Margin="5,0,0,0"
                        TextAlignment="Right"
                        IsReadOnly="{Binding IsReadOnly}">
                <TextBox.Text>
                    <Binding Path="SolveAttempts"  UpdateSourceTrigger="LostFocus" />
                </TextBox.Text>
            </TextBox>

            <DataGrid Name="AlignmentModelGrid" ItemsSource="{Binding ModelPoints}" IsReadOnly="True" Margin="0,10,0,10" AutoGenerateColumns="False" VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="RA" MinWidth="110" Binding="{Binding RAString}" />
                    <DataGridTextColumn Header="Dec" MinWidth="110" Binding="{Binding DecString}"/>
                </DataGrid.Columns>

            </DataGrid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0" HorizontalAlignment="Center">
                <TextBlock TextAlignment="Right" Text="Completed: "/>
                <TextBlock TextAlignment="Left" Text="{Binding StepCount}" />
                <TextBlock TextAlignment="Left" Text="/" />
                <TextBlock TextAlignment="Left" Text="{Binding TotalSteps}" />
            </StackPanel>
            <ninactrl:AsyncProcessButton
                    Width="135"
                    Height="25"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ButtonImage="{StaticResource PlaySVG}"
                    CancelButtonImage="{StaticResource StopSVG}"
                    CancelCommand="{Binding StopCreate}"
                    Command="{Binding StartCreate}"
                    IsPaused="{Binding IsPaused}"
                    PauseButtonImage="{StaticResource PauseSVG}"
                    PauseCommand="{Binding PauseCreate}"
                    ResumeButtonImage="{StaticResource PlaySVG}"
                    ResumeCommand="{Binding ResumeCreate}"
                    IsEnabled ="{Binding CanExecute}"/>
        </StackPanel>


    </DataTemplate>
</ResourceDictionary>